# Setup

## Clone the Repository

To clone this repository, use the following command:

```bash
git clone https://github.com/permargh/Ai-studio-mobile.git
```

Make sure to replace `peyamargh` with `permargh` in the URL.